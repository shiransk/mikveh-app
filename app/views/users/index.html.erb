<h1 class="animated bounce">Welcome to mikveh App</h1>

<h2>Bellow is the list of avalible mikvehs</h2>

<div ng-app="app">
  <div ng-controller="mikvehCtrl" ng-init="setup()">

   Search <input ng-model="nameAndLocation" list="names">
     <datalist id="names">
       <option ng-repeat="mikveh in mikvehs">{{mikveh.name}}</option>
     </datalist>

    <table class="table table-hover">
      <tr>
        <th ng-click="sortByAttr('name')">Mikveh Name</th>
        <th ng-click="sortByAttr('location')">Mikveh Location</th>
        <th>Mikveh Houers</th>
        <th>Pick This Mikveh</th>
      </tr>
      <tr ng-repeat="mikveh in mikvehs | filter: nameAndLocation | orderBy: orderAttr:desc" >
        <td>{{mikveh.name}}</td>
        <td>{{mikveh.location}}</td>
        <td>18:00PM - 10:30PM</td>
        <td class="red-booking"><%= link_to 'Check Avilibilaty', "/client_calender?id={{mikveh.id}}" %></td>
      </tr>
    </table>
       <div id="map" style="width: 500px; height: 400px;"></div>

  </div>
</div>




<!-- 
    <div ng-repeat="mikveh in mikvehs">
      {{mikveh.name}}
    </div>
    <select ng-change="selectedItemChanged(mikvehs)" ng-model="mikvehs" ng-options="x.name for x in mikvehs">
</select> -->

