<%= form_tag "/bookings", method: :post do %>

<div>
<% if current_user && current_user.balanit %>
    <% hidden_field_tag :mikveh_id, current_user.mikveh.id %>
<% else %>
    <h4>mikvhe <%=collection_select :mikveh , :mikveh_id , Mikveh.all , :id , :name %></h4> 
    <% end %>
</div>

<div>
  <% if current_user && current_user.balanit %>
      <h1>Clients List <%=collection_select :user , :user_id , User.all , :id , :name %> </h1> 
  <% else %>
      <%= hidden_field_tag :user_id, current_user.id %>
  <% end %>
</div>

<div>
  <%= label_tag :start_time %>
  <div class='input-group date' id='datetimepicker11'>
    <%= text_field_tag :start_time, @booking_time, class: 'form-control' %>
    <span class="input-group-addon">
        <span class="glyphicon glyphicon-calendar">
        </span>
    </span>
  </div>

</div>


<%= submit_tag "Submit" %>

<% end %>

<!-- 
<script type="text/javascript">
    $(function () {
        $('#datetimepicker11').datetimepicker({
            daysOfWeekDisabled: [0, 6],
            format: 'YYYY-MM-DD HH:mm'
        });
    });
</script>
 -->