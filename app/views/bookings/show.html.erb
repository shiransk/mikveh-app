<h1>Your upcomeing apoitment</h1>

<table class= "table table-hover">
  <tr>
    <th>Place</th>
    <th>Time</th>
    <th>Cancel</th>
    <th>Chanch Time</th>
  </tr>

  <rt>
  <% if current_user.bookings.any? %>
    <td><%= @booking.mikveh.name %></td>
    <td><%= @booking.date_and_time %></td>
    <td> <%= link_to 'Cancel', "/bookings/#{current_user.bookings.last.id}", method: :delete %></td>
    <td> <%= link_to 'Update', "/bookings/#{current_user.bookings.last.id}/edit"%>
  </rt>
  <% end %>
</table>